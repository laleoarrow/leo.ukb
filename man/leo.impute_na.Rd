% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analyze.R
\name{leo.impute_na}
\alias{leo.impute_na}
\title{Basic imputation function for missing values}
\usage{
leo.impute_na(data, method = "mean", ...)
}
\arguments{
\item{data}{A data frame containing the data with missing values}

\item{method}{Imputation method: "mean", "median", "rf" (random forest),
"knn" (K-nearest neighbors), or "mice" (multiple imputation)}

\item{...}{Additional arguments passed to the specific imputation function}
}
\value{
A data frame with missing values imputed
}
\description{
This function provides multiple methods for imputing missing values in a dataset,
including mean, median, random forest, KNN, and multiple imputation.
}
\examples{
# Create sample data with missing values
set.seed(123)
sample_data <- data.frame(
  age = c(25, 30, NA, 40, 45),
  score = c(80, NA, 90, 85, NA),
  group = factor(c("A", "B", "A", NA, "B"))
)

# Mean imputation
leo.impute_na(sample_data, method = "mean")

# Random forest imputation
leo.impute_na(sample_data, method = "rf")
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dx.R
\name{dx_status}
\alias{dx_status}
\title{Check DNAnexus Job Status & Dashboard}
\usage{
dx_status(all_projects = TRUE, job_id = NULL, limit = 5)
}
\arguments{
\item{all_projects}{Logical. If TRUE, shows jobs from all projects in the account (account-wide).
Default is FALSE (only current project).}

\item{job_id}{Optional. A string containing the job ID (e.g., "job-Gk2...") or a list
returned by \code{\link{dx_extract}}. If provided, \code{all_projects} is ignored.}

\item{limit}{Numeric. Number of recent jobs to show in the dashboard (default: 3).}
}
\value{
The state of the job as a string (if \code{job_id} provided) or a data frame
of recent jobs (if \code{job_id} is NULL), invisibly.
}
\description{
Checks the status of a specific job or displays a dashboard of recent jobs.
}
\details{
When called without a \code{job_id}, it displays a "htop"-style dashboard of the
last several jobs.
}
\examples{
\dontrun{
# 1. View dashboard of recent jobs in current project
dx_status()

# 2. View dashboard for ONLY the current project
dx_status(all_projects = FALSE)

# 3. Check status of a specific job
dx_status(job_id = "job-Gk2PzX0Jj1X4Y5Z6")
}
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analyze.R
\name{leo_cox_subgroup}
\alias{leo_cox_subgroup}
\alias{leo_cox_subgroup_format}
\title{Cox subgroup analysis}
\usage{
leo_cox_subgroup(
  df,
  y_out,
  x_exp,
  x_subgroup,
  x_cov = NULL,
  event_value = 1,
  min_followup_time = 0,
  x_exp_type = "auto",
  x_subgroup_type = "categorical",
  add_interaction = TRUE,
  add_heterogeneity = FALSE,
  verbose = TRUE
)

leo_cox_subgroup_format(x, style = "wide")
}
\arguments{
\item{df}{Data frame containing the outcome, follow-up time, exposure, subgroup variables, and covariates.}

\item{y_out}{Character vector of length 2 giving the event and follow-up time column names: \code{c(event, time)}.}

\item{x_exp}{Character scalar giving the exposure column name.}

\item{x_subgroup}{Character vector giving one or more subgroup column names.}

\item{x_cov}{\code{NULL}, a character vector of covariate column names, or a list of covariate column-name vectors.}

\item{event_value}{Value in the event column that indicates incident events.}

\item{min_followup_time}{Numeric scalar; keep rows with \code{time > min_followup_time}.}

\item{x_exp_type}{Exposure type handling for \code{x_exp}.}

\item{x_subgroup_type}{Type handling for \code{x_subgroup}. Subgroup analyses are expected to use categorical grouping variables.}

\item{add_interaction}{Logical; whether to append \verb{P for interaction} from \code{leo_cox_interaction()}.}

\item{add_heterogeneity}{Logical; whether to calculate a heterogeneity P value from subgroup summary statistics.}

\item{verbose}{Logical; print progress messages.}

\item{x}{Result returned by \code{leo_cox_subgroup()}.}

\item{style}{One of \code{"wide"} or \code{"tidy"}.}
}
\value{
A \code{leo_cox_subgroup} object containing a subgroup result table in
\verb{$result} and, when requested, interaction test objects in \verb{$interaction}.
When \code{x_subgroup} has length 1, \verb{$interaction} is a single
\code{leo_cox_interaction} object; otherwise it is a named list of
\code{leo_cox_interaction} objects keyed by subgroup variable.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}
}
\details{
\code{leo_cox_subgroup()} runs \code{leo_cox()} within each level of one or more
subgroup variables and appends a \verb{P for interaction} from
\code{leo_cox_interaction()}. Optionally, it also calculates a summary-statistic
heterogeneity P value using \code{leo_heterogeneity_p()}.
}
\examples{
lung_df <- stats::na.omit(
  dplyr::transmute(
    survival::lung,
    outcome = as.integer(status == 2),
    outcome_censor = time / 365.25,
    age = age,
    sex = factor(sex, levels = c(1, 2), labels = c("Male", "Female")),
    ecog_group = factor(ph.ecog, levels = 0:3, labels = c("ECOG0", "ECOG1", "ECOG2", "ECOG3"))
  )
)

model <- list("Crude" = NULL, "Model A" = c("ecog_group"))
res_sub <- leo_cox_subgroup(
  df = lung_df, y_out = c("outcome", "outcome_censor"),
  x_exp = "age", x_subgroup = "sex", x_cov = model, verbose = FALSE
)
res_sub$result
leo_cox_subgroup_format(res_sub, style = "wide")
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analyze.R
\name{leo_cox_regression}
\alias{leo_cox_regression}
\alias{leo_cox_regression_format}
\title{Cox regression fitting and formatting helpers}
\usage{
leo_cox_regression(
  df,
  y_out,
  x_exp,
  x_cov = NULL,
  event_value = 1,
  min_followup_time = 0,
  x_exp_type = "auto",
  verbose = TRUE
)

leo_cox_regression_format(x, style = "wide")
}
\arguments{
\item{df}{Data frame containing the outcome, follow-up time, exposure, and covariates.}

\item{y_out}{Character vector of length 2 giving the event and follow-up time
column names: \code{c(event, time)}.}

\item{x_exp}{Character scalar giving the exposure column name.}

\item{x_cov}{\code{NULL}, a character vector of covariate column names, or a list
of covariate column-name vectors.}

\item{event_value}{Value in the event column that indicates incident events.}

\item{min_followup_time}{Numeric scalar; keep rows with \code{time > min_followup_time}.
Default is \code{0}, which excludes pre-baseline or baseline events when defining
an incident outcome.}

\item{x_exp_type}{Exposure type handling for \code{x_exp}. Use \code{"auto"} to infer from the input
type, \code{"continuous"} to force a numeric Cox term, or \code{"categorical"} to
force factor coding. In \code{"auto"} mode, small integer-coded exposures will
trigger a warning because they may represent categorical groups.}

\item{verbose}{Logical; print progress messages.}

\item{x}{Result returned by \code{leo_cox_regression()}.}

\item{style}{One of \code{"wide"}, \code{"tidy"}, or \code{"gtsummary"}.}
}
\value{
A \code{leo_cox_regression} object containing the default wide table in
\verb{$result}, the underlying tidy rows in \verb{$result_tidy}, model metadata, and
optional fitted \code{coxph} objects.
}
\description{
\code{leo_cox_regression()} fits one or more Cox proportional hazards models for a
single exposure and incident outcome. \code{leo_cox_regression_format()} converts
the returned object into a wide summary table, tidy result table, or
\code{gtsummary} output.
}
\section{Formatting output}{

\code{leo_cox_regression_format()} returns:
\itemize{
\item \code{"wide"}: a wide summary data frame with one set of HR / CI / p-value
columns per model.
\item \code{"tidy"}: the row-level tidy result table stored in \code{x$result_tidy}, with
formatted display columns added.
\item \code{"gtsummary"}: a \code{gtsummary} regression table, or a merged \code{gtsummary}
table when multiple models are present.
}
}

\examples{
lung_df <- stats::na.omit(
  dplyr::transmute(
    survival::lung,
    outcome = as.integer(status == 2),
    outcome_censor = time,
    age = age,
    sex = factor(sex, levels = c(1, 2), labels = c("Male", "Female")),
    ph.ecog = factor(ph.ecog)
  )
); head(lung_df)

model <- list(
  crude = NULL,
  model_a = c("sex"),
  model_b = c("sex", "ph.ecog")
)

res <- leo_cox_regression(
  df = lung_df,
  y_out = c("outcome", "outcome_censor"),
  x_exp = "age",
  x_cov = model
)

leo_cox_regression_format(res, style = "wide")
leo_cox_regression_format(res, style = "tidy")
if (requireNamespace("gtsummary", quietly = TRUE) && requireNamespace("broom.helpers", quietly = TRUE)) {
  leo_cox_regression_format(res, style = "gtsummary")
}
}
